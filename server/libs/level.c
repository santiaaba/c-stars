#include "level.h"

void level_init(level_t *level){
	lista_init(level->secuence, sizeof(attak_t))
	level->background = 0;
	level->soundTrak = 0;
}

void level_load(level_t *level){
}

void level_run(level_t *level){
}
